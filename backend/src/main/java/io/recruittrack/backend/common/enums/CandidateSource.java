package io.recruittrack.backend.common.enums;

/** How a candidate was sourced. Stored as VARCHAR(50). */
public enum CandidateSource {
    LINKEDIN,
    REFERRAL,
    JOB_PORTAL,
    CAREER_SITE,
    AGENCY,
    DIRECT_APPLICATION,
    OTHER
}
