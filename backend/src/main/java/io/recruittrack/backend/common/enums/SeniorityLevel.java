package io.recruittrack.backend.common.enums;

/** Seniority level for a job posting. Stored as VARCHAR(30). */
public enum SeniorityLevel {
    ENTRY,
    MID,
    SENIOR,
    LEAD,
    PRINCIPAL,
    DIRECTOR
}
